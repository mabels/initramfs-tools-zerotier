This repo enables zerotier - ipn in the initramfs boot disk.

You need to install zerotier and dropbear-initramfs on your system before.
Than copy this repo to /etc/initramfs-tools

# initramfs-tools-zerotier
